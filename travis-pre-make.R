
if (!requireNamespace("devtools", quietly = TRUE) ||
    !requireNamespace("hunspell", quietly = TRUE) || 
    !requireNamespace("haven", quietly = TRUE)) {
  file.create("do_install_hunspell_etc")
}
